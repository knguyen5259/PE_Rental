import SwiftUI

struct ContentView: View {
    
    var body: some View {
        VStack {
           
            Button {
                print("Button pressed")
            } label: {
                Text("Uibutton ")
            }

        }
    }
}
